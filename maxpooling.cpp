#include "maxpooling.h"

std::vector<cv::Mat> MaxPooling::perform(std::vector<cv::Mat> in)
{
    for (int l = 0; l < in.size(); l++){
        for (int r = 0; r < in[l].rows; r++){
            for (int c = 0; c < in[l].cols; c+=2){

            }
        }
    }

}
